
# KD6POX Blog
1. In the repository settings (Settings → Pages), choose “GitHub Actions” as the Source if it isn’t already.
2. Push to `main` (or use “Run workflow”) to trigger the deployment. The published URL will appear in the workflow run output under the “Deploy to GitHub Pages” step.
